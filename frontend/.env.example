# Copy to .env.local for local dev (Vite loads .env.local automatically).
VITE_API_BASE_URL=http://localhost:8000
# Optional: URL of the external transaction producer's inject endpoint.
# Leave unset to hide/disable the "Inject" control.
VITE_INJECT_URL=
